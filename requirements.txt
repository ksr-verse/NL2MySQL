# NL2MySQL - Python Dependencies Installation File
#
# WHAT THIS FILE IS:
# This file lists all Python packages needed to run the NL2MySQL system
#
# HOW TO USE:
# 1. Create virtual environment: python -m venv venv
# 2. Activate it: venv\Scripts\activate (Windows) or source venv/bin/activate (Linux/Mac)
# 3. Install dependencies: pip install -r requirements.txt
# 4. Configure: cp env.example .env (then edit .env with your API key)
# 5. Run: python app.py
#
# WHAT GETS INSTALLED:
# - FastAPI (web framework)
# - ChromaDB (vector database)
# - MySQL connectors (database)
# - Streamlit (web interface)
# - Groq API dependencies
#
# Groq-Only Setup - No local LLM dependencies needed

# Core API framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database connectivity
sqlalchemy==2.0.23
mysql-connector-python==8.2.0
PyMySQL==1.1.0
pandas==2.1.4

# Vector database for schema embeddings
chromadb==0.4.18
sentence-transformers==2.2.2

# HTTP requests for Groq API
requests==2.31.0

# SQL parsing and validation
sqlparse==0.4.4

# Streamlit web interface
streamlit==1.28.1

# Utilities
python-dotenv==1.0.0
loguru==0.7.2
